{
  "name": "mern-project",
  "version": "1.0.0",
  "description": "mern based project",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch api/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup-project": "npm i && cd client && npm i",
    "setup-production-app": "npm i && cd client && npm i && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
